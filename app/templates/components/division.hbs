<@layers.geoJSON @geoJSON={{@feature}} as |layer|>
  <layer.popup>
    <strong>{{@feature.properties.ED}}</strong>
    <br>
    <br>
    {{#each this.candidates as |candidate|}}
      {{candidate}}<br>
    {{/each}}
  </layer.popup>
</@layers.geoJSON>
